{
  "name": "hh-lib",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "save": "git add --all && git commit -m 'feat: 更新代码' && git push origin main"
  },
  "version": "0.0.1",
  "description": "A lib",
  "main": "index.js",
  "author": "hadeshe",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  }
}
